# 코딩/구조 컨벤션

이 문서는 카테고리와 스크립트가 늘어날수록 계속 업데이트됩니다.
현재는 초안이며, 실제로 코드를 추가하면서 맞지 않는 규칙은 고쳐나갈 예정입니다.

## 1. 폴더 구조

새 업무 카테고리를 추가할 때는 아래 구조를 그대로 따릅니다.

```
<category>/
├── sample_data/     # 실행 가능한 가짜 예제 데이터 (필수)
├── output/          # 스크립트 실행 결과물 (git에는 커밋하지 않음, .gitkeep만 유지)
├── README.md        # 이 카테고리가 뭘 하는지 3~5줄 요약 + 실행법
└── <task_name>.py   # 실제 스크립트 (카테고리당 여러 개 가능)
```

- 카테고리 폴더명은 영어 소문자, 스크립트 파일명은 `snake_case.py`
- 카테고리 = 경영지원팀 업무 대분류 (finance, hr, procurement, general_affairs, legal ...)
- 하나의 스크립트는 하나의 업무 시나리오만 담당한다 (여러 기능을 억지로 합치지 않는다)

## 2. 스크립트 작성 규칙

- 파일 맨 위에 **docstring**으로 다음을 반드시 명시: 용도, 사용법(CLI 예시), 입력 컬럼, 출력 파일
- `argparse`로 `--input` 등 입력 경로를 받는다 (하드코딩 금지). 기본값은 `sample_data/`의 샘플 파일로 지정해서, 옵션 없이 실행해도 바로 동작해야 한다.
- 회사마다 다른 값(계정과목 이름, 부서명, 기준일수 등)은 파일 상단에 상수로 분리해서 다른 조직 데이터에도 쉽게 맞출 수 있게 한다.
- `common/` 모듈(엑셀 IO, 차트 스타일, 포맷팅)을 우선 재사용하고, 카테고리 전용 로직만 각 스크립트에 둔다.
- 함수/변수명은 영어, 주석과 print 출력은 한글로 통일한다.
- `main()` 함수 + `if __name__ == "__main__":` 패턴을 지킨다.
- 타입힌트를 붙인다 (`from __future__ import annotations` 사용).

## 3. 샘플 데이터 규칙

- 실제 회사 데이터/개인정보는 절대 커밋하지 않는다. 샘플 데이터는 반드시 가상의 인물/거래처명으로 생성한다.
- 샘플 데이터는 스크립트를 옵션 없이 실행했을 때 "그럴듯한" 결과(임박 계약, 지각 사례 등 이상치 포함)가 나오도록 구성한다. 전부 정상 범위인 데이터는 리포트의 강조/알림 로직을 검증할 수 없다.
- 데이터 생성에 재현성이 필요하면 랜덤 시드를 고정한다.

## 4. 출력 규칙

- 표 형태 결과 → `output/*.xlsx` (엑셀, 담당자가 바로 열어볼 수 있게)
- 시각화 결과 → `output/*.png`
- 콘솔 출력은 `common/format_utils.py`의 `print_section`, `krw`, `pct` 등을 사용해 톤을 통일한다.
- README에 넣을 대표 이미지 1장은 `docs/screenshots/`에 `<category>_<설명>.png`로 별도 보관한다 (output/은 gitignore 대상이라 README에서 깨지기 때문).

## 5. 새 카테고리/스크립트 추가 체크리스트

- [ ] `sample_data/`에 실행 가능한 샘플 데이터 포함
- [ ] 옵션 없이 `python <category>/<script>.py` 실행 시 에러 없이 완료
- [ ] 엑셀/차트 결과물이 실제로 생성되는지 확인
- [ ] 카테고리 `README.md`에 실행법 기재
- [ ] 루트 `README.md`의 예제 표 + 카테고리 목록 갱신
- [ ] 이 규칙과 안 맞는 부분이 있으면 이 문서(CONVENTIONS.md)도 함께 수정

## 6. 향후 논의 예정

- Node.js/TypeScript 도입 여부: 슬랙 알림, 웹훅, 대시보드성 웹앱처럼 실시간 연동이 필요한 업무가 생기면 그 부분만 별도 스택(`integrations/` 등)으로 추가하는 것을 검토한다. 문서/엑셀 중심 업무는 계속 Python으로 유지한다.
- 스크립트가 많아지면 카테고리별 `requirements.txt` 분리 여부 재검토.
