# 코딩/구조 컨벤션

이 문서는 카테고리와 스크립트가 늘어날수록 계속 업데이트됩니다.
현재는 초안이며, 실제로 코드를 추가하면서 맞지 않는 규칙은 고쳐나갈 예정입니다.

## 1. 폴더 구조

새 업무 카테고리를 추가할 때는 아래 구조를 그대로 따릅니다.

```
<category>/
├── sample_data/     # 실행 가능한 가짜 예제 데이터 (필수)
├── output/          # 스크립트 실행 결과물 (git에는 커밋하지 않음, .gitkeep만 유지)
├── README.md        # 이 카테고리가 뭘 하는지 3~5줄 요약 + 실행법
└── <task_name>.py   # 실제 스크립트 (카테고리당 여러 개 가능)
```

- 카테고리 폴더명은 영어 소문자, 스크립트 파일명은 `snake_case.py`
- 카테고리 = 경영지원팀 업무 대분류 (finance, hr, procurement, general_affairs, legal ...)
- 하나의 스크립트는 하나의 업무 시나리오만 담당한다 (여러 기능을 억지로 합치지 않는다)

> **현재 상태 메모**: finance/hr/procurement 모두 카테고리별 `README.md`가
> 아직 없다(체크리스트 8번 항목 미충족). 새 카테고리를 늘리기 전에 정리하는
> 걸 권장.

## 2. 스크립트 작성 규칙

- 파일 맨 위에 **docstring**으로 다음을 반드시 명시: 용도, 사용법(CLI 예시), 입력 컬럼, 출력 파일
- `argparse`로 `--input` 등 입력 경로를 받는다 (하드코딩 금지). 기본값은 `sample_data/`의 샘플 파일로 지정해서, 옵션 없이 실행해도 바로 동작해야 한다.
- 회사마다 다른 값(계정과목 이름, 부서명, 기준일수 등)은 파일 상단에 상수로 분리해서 다른 조직 데이터에도 쉽게 맞출 수 있게 한다.
- `common/` 모듈(엑셀 IO, 차트 스타일, 포맷팅, 기간 계산, 세금 추정, 시트 연동, 알림 연동, PDF 렌더링)을 우선 재사용하고, 카테고리 전용 로직만 각 스크립트에 둔다.
- 함수/변수명은 영어, 주석과 print 출력은 한글로 통일한다.
- `main()` 함수 + `if __name__ == "__main__":` 패턴을 지킨다.
- 타입힌트를 붙인다 (`from __future__ import annotations` 사용).

## 3. 데이터 소스: 로컬 파일 vs 실시간 연동 (구글 시트 등)

일부 업무는 실무에서 로컬 엑셀이 아니라 구글 스프레드시트처럼 여러 명이
동시에 채우는 데이터를 다룬다. 이런 카테고리는 아래 패턴을 따른다.

- 실시간 연동에 필요한 값(시트 ID, 서비스 계정 키 경로, 탭 이름 등)은 **코드에
  하드코딩하지 않고 `.env`로 분리**한다. 회사/계정이 바뀌어도 `.env` 값만
  새로 채우면 되고 코드는 건드리지 않는다 (`.env.example` 참고).
- `.env`가 없거나 필요한 값이 비어있으면 **자동으로 `sample_data/`의 로컬
  파일로 폴백**한다. 즉 자격증명이 없는 사람이 클론해도 항상 실행은 된다.
- 연동 커넥터(예: `common/sheet_io.py`)는 특정 카테고리 전용이 아니라
  `common/`에 두고 여러 카테고리가 재사용할 수 있게 만든다.
- `.env`, 서비스 계정 키(json) 등 비밀값은 반드시 `.gitignore`에 포함한다.

## 4. 알림 연동 (이메일 / 슬랙 / 팀즈)

계약만료 임박, 지연 태스크처럼 "누군가 챙겨야 하는" 이벤트가 있는
카테고리는 `common/notify_utils.py`를 통해 알림을 보낸다. 3번(구글시트)
패턴과 동일한 철학을 따른다.

- 채널 선택은 `.env`의 `NOTIFY_CHANNEL`(email 기본값 / slack / teams)로
  정하고, 채널별 자격증명도 전부 `.env`로 분리한다.
- 자격증명이 없으면 에러 없이 **콘솔 출력 + 로그 파일(csv) 기록**으로
  대체한다 — 알림 설정을 안 해도 스크립트 자체는 항상 끝까지 돈다.
- 알림은 건별로 스팸처럼 보내지 않고, 한 번 실행에 대해 **하나의 요약
  메시지**로 모아 보낸다 (예: 만료 임박 계약 N건을 한 통으로).

## 5. 판단 기준의 출처

"위험군", "이상치", "집중도" 같은 걸 가르는 임계값은 가능하면 **직접
지어내지 않고 공신력 있는 외부 기준을 그대로 인용**하고, 코드 docstring과
리포트에 출처를 명시한다. 근거가 있어야 CEO/현업이 결과를 신뢰하고, 나중에
기준이 바뀌어도(법 개정 등) 어디를 고쳐야 하는지 명확하다.

- 예: HR 야근 위험단계는 임의의 배수가 아니라 근로복지공단 과로사(업무상
  질병) 인정기준(4주 평균 64h / 12주 평균 60h·52h)을 그대로 사용
  (`hr/attendance_pattern_analysis.py`).
- 예: 구매처 집중도는 공정거래위원회·미국 DOJ/FTC가 쓰는 HHI(허핀달-허쉬만
  지수) 계산식과 임계값(1500/2500)을 그대로 차용
  (`procurement/vendor_purchase_analysis.py`).
- 마땅한 공식 기준이 없는 경우에만 자체 임계값을 쓰되, 그 경우엔 "왜 이
  숫자인지"를 docstring에 반드시 남긴다.

## 6. 세금/법률 관련 계산 규칙

- 법인세율, 부가세율처럼 법 개정으로 바뀌는 값은 **연도별 표(dict)로 분리**해서
  세율이 바뀌어도 표만 갱신하면 되도록 만든다 (`common/tax_utils.py`의
  `CORPORATE_TAX_BRACKETS` 참고).
- 이런 계산 결과는 전부 **추정치**임을 코드 docstring과 화면 출력에 명시한다.
  세무조정, 이월결손금 등 실제 신고 과정의 조정 사항은 반영하지 않는다는
  점을 반드시 고지한다.
- 부가세처럼 손익계산서 항목이 아닌 세무 정보는 손익계산서 안에 억지로
  섞지 않고 별도 시트/섹션으로 분리한다.

## 7. 기간 비교 규칙

- 여러 시간 단위(년/반기/분기/월/주)를 지원하는 스크립트는 **같은 단위끼리만
  비교**한다. 단위가 다른 기간(예: 주 vs 월)은 비교 자체를 만들 수 없게
  설계한다 (`common/period_utils.py` 참고).
- "직전기"와 "전년 동기"는 항상 함께 제공해서, 단기 변동(직전기)과 계절성을
  걸러낸 변동(전년동기)을 같이 볼 수 있게 한다.
- 비교 대상 기간에 데이터가 없으면 에러를 내지 않고 "데이터 없음"으로
  표시한다.

## 8. 샘플 데이터 규칙

- 실제 회사 데이터/개인정보는 절대 커밋하지 않는다. 샘플 데이터는 반드시 가상의 인물/거래처명으로 생성한다.
- 여러 시간 단위 비교를 시연하는 카테고리는 최소 1년치 이상, **거래 건별(전표 단위, 날짜 포함)**로 샘플 데이터를 만든다. 이미 월별로 집계된 데이터는 주/분기 단위 비교를 시연할 수 없다.
- 샘플 데이터는 스크립트를 옵션 없이 실행했을 때 "그럴듯한" 결과(임박 계약, 지각 사례, 계절적 변동 등 이상치 포함)가 나오도록 구성한다. 전부 정상 범위인 데이터는 리포트의 강조/알림 로직을 검증할 수 없다.
- 이상탐지/패턴탐지 기능이 있는 카테고리는 **탐지돼야 할 사례를 의도적으로
  심어서** 샘플 데이터를 만든다(예: 특정 인원 요일 집중 지각, 특정 부서
  특정월 급증). 그래야 "이 도구가 실제로 뭔가 잡아낸다"를 실행 한 번으로
  보여줄 수 있다.
- 데이터 생성에 재현성이 필요하면 랜덤 시드를 고정한다.

## 9. 출력 규칙

- 표 형태 결과 → `output/*.xlsx` (엑셀, 담당자가 바로 열어볼 수 있게).
  전체/부분집합을 나눠 봐야 하는 표(예: 전체/위험군/이상패턴)는 시트를
  따로 만들지 말고 상태 컬럼(Y/N)을 추가한 뒤 `save_excel_report`의
  `autofilter=True`(기본값)로 엑셀 자체 필터 버튼을 쓰게 한다.
- 시각화 결과 → `output/*.png`. 차트는 어디까지나 표를 보조하는 시각적 판단
  도구이며, CEO가 바로 알아볼 수 있는 정식 서식(K-IFRS 손익계산서 등)의
  표가 항상 우선이다.
- **이미지 화질/크기**: `common/chart_style.py`의 `save_chart()`가
  dpi=200으로 고정 저장한다. 행 수가 적어 캔버스가 작아지는 표 형태
  이미지(`ax.table` 사용)는 `bbox=[0, 0, 1, 1]`로 축 전체를 채워서 불필요한
  여백이 남지 않게 하고, README에 나란히 들어가는 다른 대표 이미지들과
  가로세로 비율(대략 1.6~2:1)이 비슷하게 figsize를 잡는다 — 한 이미지만
  유난히 작거나 납작하게 보이지 않도록.
- 콘솔 출력은 `common/format_utils.py`의 `print_section`, `krw`, `pct`,
  `signed_krw` 등을 사용해 톤을 통일한다. 값이 없을 수 있는 자리는 반드시
  `None`/`NaN`을 처리해서 "N/A"로 보이게 한다 (`nan%` 같은 값이 그대로
  노출되면 안 됨).
- README에 넣을 대표 이미지 1장은 `docs/screenshots/`에 `<category>_<설명>.png`로 별도 보관한다 (output/은 gitignore 대상이라 README에서 깨지기 때문). 새 모듈을 추가하거나 기존 대표 이미지의 내용이 바뀌면, 이 이미지를 새로 저장하고 README의 "실행 결과 미리보기"에 어떤 파일을 넣어야 하는지 매번 안내한다(README 본문 편집은 사용자가 직접 하지만 이미지와 배치 안내는 빠짐없이 챙긴다). 스크립트가 교체/삭제된 경우 README의 예제 표·시작하기 명령어에 옛 이름이 남아있지 않은지도 같이 확인한다.

## 10. 새 카테고리/스크립트 추가 체크리스트

- [ ] `sample_data/`에 실행 가능한 샘플 데이터 포함 (이상탐지 기능이 있다면 탐지될 사례 의도적으로 포함)
- [ ] 옵션 없이 `python <category>/<script>.py` 실행 시 에러 없이 완료
- [ ] 엑셀/차트 결과물이 실제로 생성되는지 확인 (차트는 dpi/비율 기준 충족 여부까지)
- [ ] 여러 기간/조건으로 다시 실행해서 경계값(데이터 없음, 0으로 나누기 등)에서 안 깨지는지 확인
- [ ] 위험도/이상치 판단 기준을 새로 만들었다면 출처(5번 규칙) 명시했는지 확인
- [ ] 카테고리 `README.md`에 실행법 기재
- [ ] 루트 `README.md`의 예제 표 + 카테고리 목록 + 대표 이미지 갱신
- [ ] 이 규칙과 안 맞는 부분이 있으면 이 문서(CONVENTIONS.md)도 함께 수정

## 11. 향후 논의 예정

- Node.js/TypeScript 도입 여부: 슬랙 알림, 웹훅, 대시보드성 웹앱처럼 실시간 연동이 필요한 업무가 생기면 그 부분만 별도 스택(`integrations/` 등)으로 추가하는 것을 검토한다. 문서/엑셀 중심 업무는 계속 Python으로 유지한다.
- 스크립트가 많아지면 카테고리별 `requirements.txt` 분리 여부 재검토.
- finance/hr/procurement/general_affairs 카테고리별 `README.md` 작성 (1번 체크리스트 미충족 상태로 밀려 있음).
- HR/구매·계약/총무도 재무처럼 Colab 인터랙티브 노트북 추가할지 검토.
- legal(법무) 카테고리는 아직 미착수.
