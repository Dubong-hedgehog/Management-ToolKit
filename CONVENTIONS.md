# CONVENTIONS.md

이 저장소에 코드를 추가/수정할 때 지키는 규칙. 새 카테고리나 스크립트를 추가할 때마다
이 문서도 함께 갱신합니다.

## 1. 프로젝트 목적 & 기술 스택

경영지원팀(회계·재무 / 인사·총무 / 구매·계약 / 법무) 실무 자동화 코드 모음이자 개인
포트폴리오. 기본은 Python이고, 슬랙 알림·웹훅처럼 실시간 연동이 필요할 때만 Node.js/TS를
부분적으로 추가합니다. 문서·엑셀·데이터 집계 업무는 항상 Python을 씁니다.

## 2. 설계 전 확인사항

새 기능을 만들기 전에 "이게 실제로 어떻게 소비되는지"부터 확인합니다. 정적 파일(README에
박히는 스크린샷, 한 번 생성해두는 예시 PDF)로 충분한지, 아니면 사용자가 화면에서 데이터를
보다가 그 순간 선택된 조건으로 즉석 생성/다운로드되어야 하는지는 완전히 다른 설계입니다.
이 판단을 건너뛰면 나중에 "이건 어떻게 보는거냐"는 질문이 나와서 다시 뜯어고쳐야 하므로,
사용 시나리오(어디서, 누가, 어떤 트리거로 결과를 받는지)를 먼저 확인한 뒤 설계합니다.

## 3. 코드 작성 원칙

- 모든 스크립트는 실제로 실행 가능해야 함(뼈대만 있는 코드 금지). 샘플 데이터를 포함해서
  clone 후 명령어 한 줄이면 실제 결과(엑셀/차트/PDF)가 나와야 함.
- 계정과목명, 부서명 등 회사마다 다른 값은 상수로 분리해서 다른 조직 데이터에도 재사용
  가능하게 작성.
- 공통 유틸(엑셀 입출력, 차트 스타일, 포맷팅, 기간 계산, 세금 추정, 시트 연동, PDF 렌더링,
  알림, LLM 연동)은 `common/` 모듈로 분리해서 재사용.
- 함수/변수명은 영어, 주석·문서·콘솔 출력은 한글.
- 타입힌트와 docstring(용도/사용법/입력/출력) 필수.
- 입력 경로는 argparse로 받고, 옵션 없이 실행해도 샘플 데이터로 동작해야 함.

## 4. 알림 연동

만료 임박·이상 탐지처럼 "누군가에게 알려야 하는" 결과는 `common/notify_utils.py`를 통해
보냅니다. 기본 채널은 이메일이고, `.env`의 `NOTIFY_CHANNEL`로 slack/teams 웹훅으로 바꿀 수
있습니다. `.env`가 없으면 콘솔 출력 + CSV 로그로 자동 대체되어, 알림 채널을 설정하지 않아도
스크립트는 끝까지 실행됩니다.

## 5. LLM(Claude API) 연동

이력서 정성 평가처럼 규칙 기반 매칭만으로는 부족한 판단이 필요한 경우, `common/llm_utils.py`
를 통해 Claude API를 선택적으로 사용합니다.

- `.env`의 `ANTHROPIC_API_KEY`가 설정되어 있으면 `call_claude()`로 실제 Claude 추론을 쓰고,
  `extract_json()`으로 응답에서 JSON을 안전하게 파싱합니다.
- 키가 없으면(`is_configured() == False`) 반드시 규칙 기반 폴백 로직으로 대체해서, API 키
  없이 clone해도 스크립트가 끝까지 동작해야 합니다. LLM 호출은 `try/except`로 감싸서 호출
  실패 시에도 폴백으로 넘어가게 합니다(네트워크 오류, rate limit 등).
- 실제로 API를 호출하면 사용자 본인의 Anthropic API 사용량에 따라 비용이 발생한다는 점을
  관련 스크립트 docstring에 명시합니다.
- 폴백 결과에는 "키워드 매칭 기반 근사치"라는 점을 명시적으로 표시해서, 사용자가 LLM 결과와
  폴백 결과를 혼동하지 않게 합니다.

### 5-1. 채용/평가 관련 LLM·키워드 로직의 공정성 원칙

사람을 평가하는 기능(예: `hr/fit_taxonomy.py`, `hr/build_fit_profile.py`,
`hr/resume_fit_report.py`)은 편향 이슈에 특히 취약합니다.

- 분류 카테고리와 키워드는 전부 "이력서에 서술된 행동/경험" 기준이어야 하고, 성별·나이·
  출신학교 등 인적 속성은 절대 근거로 쓰지 않습니다. LLM 프롬프트에도 이 제약을 명시적으로
  포함합니다.
- "재직중 = 핏이 좋음", "조기퇴사 = 핏이 안 좋음" 같은 근사치는 확정적 인과관계가 아니라는
  점(조직·상사·처우 문제로 퇴사했을 수 있음)을 출력물(JSON narrative, PDF 안내문 등)에 항상
  남깁니다.
- 장기재직자만 학습하면 생존편향에 빠지므로, 가능하면 대비군(조기퇴사 등 실패 사례)도 함께
  학습해서 "이미 뽑힌 사람들은 원래 비슷하다"는 착시를 줄입니다.
- 최종 산출물(리포트/프로파일)에는 "참고 자료이며 단독으로 채용을 결정하는 데 쓰면 안 된다"는
  문구를 반드시 포함합니다.

## 6. 판단 기준의 출처

위험/이상/집중도 임계값은 임의의 배수가 아니라 가능하면 공신력 있는 외부 기준을 인용하고,
출처를 docstring에 남깁니다.

- HR 야근 위험도 단계(`hr/attendance_pattern_analysis.py`): 근로복지공단 과로사 인정기준의
  52시간(법정 상한)/60시간(12주 평균 "강함")/64시간(4주 평균 "강함")을 그대로 사용.
- 구매처 집중도(`procurement/vendor_purchase_analysis.py`): HHI(Herfindahl-Hirschman Index)
  1500/2500 기준은 공정거래위원회·미국 DOJ-FTC의 기업결합 심사 기준을 차용.

새 임계값을 추가할 때도 "왜 이 숫자인지" 근거를 남기는 걸 원칙으로 합니다. 근거를 못 찾으면
그 사실도 docstring에 명시하고(추정치임을 밝힘), 나중에 더 나은 기준으로 교체할 수 있게
합니다.

## 7. D-day 알림 버킷팅

만료 임박 알림은 D-30(예고)/D-60(긴급 아님, 여유) 같은 버킷으로 분류합니다
(`procurement/contract_wbs_tracker.py`, `general_affairs/asset_lifecycle_tracker.py`,
`general_affairs/general_affairs_ledger.py`에서 재사용). 이미 만료된 항목은 "D--20"처럼
이중 음수로 표시하지 말고 "만료됨(즉시 확인 필요)" 또는 "D+N 경과" 형태로 표시합니다.

## 8. 샘플 데이터 원칙

- 실제 회사 데이터·개인정보는 절대 커밋하지 않고, 샘플 데이터는 전부 가상의 값으로 생성합니다.
- 이상/패턴 탐지 스크립트는 샘플 데이터에 탐지 대상 패턴을 의도적으로 주입해서, 옵션 없이
  한 번 실행해도 그 이상 패턴이 실제로 결과에 나타나야 합니다(그냥 랜덤 데이터만 넣고 끝내지
  않음).
- 키워드 매칭 기반 폴백 로직을 쓰는 경우, 샘플 텍스트(이력서 등)에 부정문 안에 트리거
  키워드가 그대로 들어가지 않도록 주의합니다. 예: "여러 업무를 동시에 진행하는 것은 선호하지
  않습니다"는 "여러 업무"+"동시에"가 문자열로 포함돼 있어서 단순 substring 매칭이 이걸
  긍정 신호로 잘못 읽습니다("negation-blindness" 버그). "한 번에 한 가지 업무만 처리하는
  것을 선호합니다"처럼 트리거 키워드 자체를 피해서 서술합니다.

## 9. 출력 규칙(차트/이미지/PDF)

- 차트 저장은 `common/chart_style.py`의 `save_chart()`를 쓰고 dpi는 200 이상 고정.
- 표 형태 이미지(행 수가 적어 캔버스가 작아지기 쉬운 경우)는 `figsize`를 가로 10~12in
  이상으로 넉넉히 잡고, `ax.table(...)` + `bbox=[0, 0, 1, 1]`로 여백을 없애서 글자가
  뭉개지지 않게 합니다.
- 한 카테고리 안에서 README에 들어가는 이미지들은 가로세로 비율(대략 1.6~2:1)을 서로
  비슷하게 맞춥니다.
- 카테고리마다 이미지를 꼭 1개만 보여줄 필요는 없습니다. 여러 스크립트/산출물이 있으면
  각각을 대표하는 이미지를 여러 장 보여줍니다.
- 한글 PDF는 reportlab 기본 폰트가 한글을 지원하지 않으므로, `common/pdf_statement.py`의
  `_register_korean_font()`(koreanize-matplotlib에 번들된 나눔고딕 재사용)로 폰트를 등록한
  뒤 `FONT_REGULAR`/`FONT_BOLD` 상수를 사용합니다.

## 10. README 미리보기 이미지 관리 원칙

새 모듈/스크립트를 추가하거나 기존 산출물(차트)의 내용이 바뀌면, `docs/screenshots/`에 그
결과를 대표하는 이미지를 새로 저장하고, README의 "실행 결과 미리보기"에 어떤 이미지를
넣어야 하는지 챙깁니다.

- 이미지 파일명 규칙: `{카테고리}_{내용}.png` (예: `finance_revenue_trend.png`,
  `procurement_contract_gantt.png`, `hr_fit_category_comparison.png`).
- 화질 기준은 9번과 동일(dpi 200 이상, 표 이미지는 figsize 넉넉히).
- 스크립트/기능이 교체되면 옛 이미지 파일을 방치하지 말고 정리 여부를 안내합니다. (현재
  `docs/screenshots/hr_late_by_dept.png`는 `hr_person_overview.png`로 대체된 이후 쓰이지
  않는 파일입니다 - 샌드박스 권한상 자동 삭제가 안 되어 사용자가 직접 정리해야 합니다.)
- 스크립트 파일명이 바뀌거나 삭제된 경우, README의 예제 표/시작하기 명령어에 옛 이름이
  남아있으면 반드시 짚어서 알려줍니다.

## 11. 폴더 구조

카테고리별 폴더(`finance/`, `hr/`, `procurement/`, `general_affairs/`, `legal/`) 안에
`sample_data/`, (필요시) `output/`, 스크립트를 둡니다. 대화형으로 실행해볼 수 있는 예시는
`notebooks/`에 Colab 노트북으로 제공합니다.

## 진행 방식

경영지원팀 전 업무 영역을 목표로 하되 카테고리는 점진적으로 확장합니다. 새 카테고리나
스크립트를 추가할 때마다 이 문서도 함께 갱신합니다.

## 체크리스트 (새 스크립트 추가 시)

- [ ] 샘플 데이터에 탐지 대상 패턴이 실제로 나타나는지 확인했는가
- [ ] 옵션 없이 실행해도 끝까지 도는가
- [ ] 회사마다 다른 값(계정과목명, 부서명 등)이 상수로 분리되어 있는가
- [ ] 공통 로직은 `common/`으로 뺐는가
- [ ] 임계값/기준에 출처가 있는가(6번), 없다면 추정치임을 명시했는가
- [ ] 알림이 필요한 결과는 `notify_utils.py`를 거치는가(4번)
- [ ] 사람을 평가/분류하는 로직이라면 공정성 원칙(5-1번)을 지켰는가
- [ ] docs/screenshots에 대표 이미지를 추가하고 README에 반영 안내를 했는가(10번)
- [ ] README 예제 표/시작하기 명령어에 새 스크립트를 추가했는가

## 향후 논의 예정 (백로그)

- HR 스크립트의 하드코딩된 컬럼명을 `COLUMN_MAP` 상수로 리팩터링(제안됨, 미착수)
- README에 Binder 배지 추가 — Colab 없이도 샘플 데이터를 바로 돌려볼 수 있는 대안(제안됨,
  미착수)
- hr/procurement/general_affairs/legal용 Colab 노트북 추가(finance만 있음)
- 카테고리별 개별 README.md(현재는 루트 README.md 하나에 전 카테고리 통합)
- 채용 파이프라인 5단계: 실 면접 음성 Q&A를 텍스트로 변환해 최종 적합도 산출에 반영(명시적으로
  보류, 요청 시 착수)
